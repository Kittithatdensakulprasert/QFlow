# Database Configuration
DATABASE_URL="postgres://username:password@localhost:5432/qflow?sslmode=disable"

# Server Configuration
PORT="3000"
JWT_SECRET="replace-with-a-long-random-jwt-secret-for-local-dev"

# Optional bootstrap users for protected role routes.
# Request and verify OTP with these phone numbers to receive admin/provider tokens.
BOOTSTRAP_ADMIN_PHONE=""
BOOTSTRAP_ADMIN_NAME="Bootstrap Admin"
BOOTSTRAP_PROVIDER_PHONE=""
BOOTSTRAP_PROVIDER_NAME="Bootstrap Provider"
